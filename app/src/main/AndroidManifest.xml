<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.excercise.designpatterns">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".creational.factorymethod.FactoryMethodActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".oop.inheritance.InheritanceActivity"></activity>
        <activity android:name=".creational.singleton.SingletonPatternActivity"></activity>
        <activity android:name=".behavioural.observer.ObserverPatternActivity" />
        <activity android:name=".behavioural.visitor.simplesample.VisitorActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>